// React
import React from "react";
import PropTypes from "prop-types";
// Begin
const Security = (props) => <div style={props.contentStyle}>[Security] Contactez le support ...</div>;
Security.propTypes = {contentStyle: PropTypes.object.isRequired};
export default Security;
